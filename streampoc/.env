# VITE_API_BASE is intentionally left unset for local dev so Vite's proxy handles API calls.
# This ensures the app works when accessed from other devices on the network (e.g., mobile).
VITE_API_BASE=
VITE_LIVEKIT_URL=wss://glivestreamer-t2otgzur.livekit.cloud
